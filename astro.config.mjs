// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://ivankniazev.dev',
  // Для custom domain на GitHub Pages не нужен base
  // base: '/',
});
